server:
  port: 8083
spring:
  application:
    name: product-service
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/pantrymate_product
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
# 유저 도메인 담당자의 시간적 여유가 있을 때 주석 해제 후 admin 계정 반영
#internal :
#  api:
#    secret: ${INTERNAL_API_SECRET}