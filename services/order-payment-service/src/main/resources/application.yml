spring:
  application:
    name: order-payment-service
  datasource:
    url: jdbc:postgresql://localhost:${POSTGRES_PORT:-5432}/pantrymate_orderpayment
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
